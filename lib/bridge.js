var internals = {};

/**
 * @class Bridge
 * @param {Object} hue the hue instance this bridge is attached to
 * @param {Object} host the IP address of the bridge
 * @constructor
 */
module.exports = internals.Bridge = function Bridge(hue, host) {}
